import React, {useState, useEffect} from "react";

const SocialLinks = (props) => {
    return (
        <div className="SocialLinksPage">
            
        </div>
    );
};

export default SocialLinks;